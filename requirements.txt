# These are the required dependencies that you need to install to be able to run this program
# Syntaxis: package==version
# To install these dependencies automatically, open a terminal in the project's root folder and run this command: pip install -r requirements.txt

Requests==2.28.2
requests_oauthlib==1.3.1
